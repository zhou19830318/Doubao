# HeyClawy 开发任务清单 (TODO)

## 阶段 1：项目初始化与基础驱动
- [ ] **搭建开发环境**：安装 ESP-IDF v5.5，配置工具链，验证 `idf.py` 可用。
- [ ] **克隆项目骨架**：将现有 HeyClawy 仓库作为基础，切换板级宏为 `CONFIG_HEYCLAWY_BOARD_WAVESHARE_AUDIO`。
- [ ] **实现板级抽象（board）**
  - [ ] 定义 `board_waveshare_audio.h`，按 HARNESS 约束配置引脚、I2C/I2S/SPI 外设、能力标志（DISPLAY=1, TOUCH=1, KNOB=0 等）。
  - [ ] 实现 `board.c` 中的初始化代码：I2C 总线初始化，IO 扩展器（TCA9555）配置（按 HARNESS 表设置方向），LCD/触摸电源控制，PA 使能。
  - [ ] 适配屏幕驱动：SPI 初始化，JD9853 驱动（基于 esp_lcd），触摸驱动 AXS5106L（I2C）。
  - [ ] 适配 RGB LED 驱动（WS2812，GPIO38）。
  - [ ] 适配 SD 卡驱动（SDMMC 1-bit，确保 SD_CS 保持高电平）。
- [ ] **基础 LVGL 移植**
  - [ ] 集成 lvgl 组件，配置显示缓冲（内部 RAM，DMA 兼容），创建 UI 任务。
  - [ ] 实现触摸轮询与 LVGL 输入设备绑定（通过 IO 扩展中断）。
  - [ ] 测试 lvgl 示例工程，确保屏幕可点亮、触摸可用。

## 阶段 2：音频子系统与唤醒词
- [ ] **音频编解码器驱动**
  - [ ] 使用 esp_codec_dev 组件初始化 ES8311（播放）和 ES7210（录音），I2S0 共享。
  - [ ] 实现 `board_audio_init()`、播放/录音接口，处理 PA_EN 时序。
  - [ ] 实现音频互斥锁（I2S 资源保护）。
- [ ] **唤醒词集成**
  - [ ] 集成 esp-sr 组件，使用 WakeNet 模型（默认 “Hey Jarvis”），存储于 SPIFFS 分区。
  - [ ] 创建唤醒检测任务，遵循唤醒/录音互斥约束（暂停/恢复）。
  - [ ] 测试唤醒词准确率，配置检测阈值。

## 阶段 3：网络与 OpenClaw 集成
- [ ] **WiFi 管理**
  - [ ] 移植 wifi_manager 组件，支持 STA 模式，自动重连。
  - [ ] 连接后配置 WiFi 功耗模式（MAX_MODEM 等）。
- [ ] **OpenClaw 客户端**
  - [ ] 实现 WebSocket 客户端（基于 esp_websocket_client），配置正确的 ping_interval（握手阶段 1s，后续 30s）。
  - [ ] 实现 ED25519 设备身份认证（复用已有 ed25519_lib），签名流程，连接握手。
  - [ ] 实现 JSON-RPC 帧解析、分片重组、请求/响应路由。
  - [ ] 实现 `chat.send`、`health`、`sessions.list`、`usage.status`、`cron.*` 等方法。
  - [ ] 处理服务器推送事件（chat delta/final/error，agent lifecycle，cron，health）。
  - [ ] 处理设备控制标签解析（`[DEVICE:...]`），执行命令并剥离。
  - [ ] 确保 `final` 覆盖缓冲（防重复）。
- [ ] **SNTP 时间同步**：启动后获取准确时间，用于日志和认证签名时间戳。

## 阶段 4：语音交互流（STT + 对话 + TTS）
- [ ] **STT 集成**
  - [ ] 实现录音流程：触发后暂停唤醒，通过 I2S 录制音频到 PSRAM 缓冲，静音检测结束录音。
  - [ ] 发送音频到云端 STT 服务（或本地）并接收文本。
  - [ ] 支持语言自动检测（例如 faster-whisper）。
- [ ] **TTS 集成**
  - [ ] 移植 TTS 客户端（EdgeTTS 或其他），HTTP 请求获取 MP3。
  - [ ] 使用 minimp3 解码 MP3，重采样（24kHz→16kHz）后通过 I2S 播放。
  - [ ] 实现 TTS 任务栈分配在 PSRAM（避免 DMA 竞争），确保与 MP3 播放互斥。
  - [ ] 支持 TTS 打断（触摸/按键）。
- [ ] **对话状态机**
  - [ ] 实现 `voice_chat.c`：唤醒→录音→STT→发送 OpenClaw→接收响应→TTS 播放→回到 IDLE。
  - [ ] 处理超时（20s 无对话退出激活）、错误重试。

## 阶段 5：MP3 音乐播放功能
- [ ] **MP3 播放器**
  - [ ] 实现 SD 卡 `/music/` 目录扫描，生成播放列表。
  - [ ] 实现 MP3 解码播放（共享 I2S），支持播放/暂停/停止/上一首/下一首/进度跳转。
  - [ ] 实现播放状态通知 UI：显示文件名、时长、进度。
  - [ ] 实现与 TTS 的互斥控制（播放 MP3 前停止 TTS，反之亦然）。
- [ ] **语音控制接口**
  - [ ] 解析 `[DEVICE:mp3=...]` 标签，触发对应播放操作。
  - [ ] OpenClaw 对话中可请求播放指定曲目。

## 阶段 6：用户界面（UI）开发
- [ ] **状态栏（顶部）**
  - [ ] WiFi 状态图标与 RSSI（可选）。
  - [ ] OpenClaw 连接状态（指示灯）。
  - [ ] 电量图标（使用 ADC 读取电池电压或占位）。
- [ ] **聊天气泡**
  - [ ] 实现消息列表，支持用户/助手不同样式（左右对齐、颜色）。
  - [ ] 每条消息显示时间戳。
  - [ ] 支持长文本自动折行和最多 N 条历史保留（可配置）。
- [ ] **底部状态栏**
  - [ ] 显示当前状态机文本：SLEEP / ARMED / IDLE / LISTENING / THINKING / SPEAKING / PLAYING_MP3。
  - [ ] 动态映射 OpenClaw 处理阶段（如 “THINKING” + “WS:chat.delta”）。
- [ ] **MP3 播放界面**
  - [ ] 播放时显示歌曲信息、进度条、控制按钮（可选，触摸控制）。
- [ ] **RGB 灯光可视化**
  - [ ] 根据状态机控制 WS2812 动画（绿色脉冲=录音，蓝色呼吸=思考，青色=说话，彩虹=音乐等）。
- [ ] **设置界面（可选）**
  - [ ] 通过 Web 服务器修改设置（音量、亮度、睡眠超时等）。

## 阶段 7：系统状态管理与电源优化
- [ ] **睡眠与唤醒**
  - [ ] 实现 sleep_task：监测空闲超时（60s）→ 进入 SLEEP（屏幕关闭、RGB 变暗、CPU 可能降频）。
  - [ ] 实现 BOOT 键唤醒：从 SLEEP 进入 ARMED，不直接启动录音。
  - [ ] 实现 20s 退出激活逻辑。
- [ ] **电源管理**
  - [ ] 配置 CPU 频率调节（CONFIG_PM_ENABLE）。
  - [ ] 在 SLEEP 期间停止 LVGL 渲染、降低轮询频率、暂停唤醒检测（可选）。
  - [ ] WiFi 功率管理（MAX_MODEM 在睡眠时启用）。
- [ ] **触摸交互**
  - [ ] 定义触摸区域/手势用于取消操作（如长按取消录音/播放）。
  - [ ] 实现取消操作的幂等性。

## 阶段 8：数据存储与日志
- [ ] **SD 卡聊天日志**
  - [ ] 格式化 SD 卡（FATFS），挂载到 VFS。
  - [ ] 实现日志追加写：按天创建文件 `/chat/YYYY-MM-DD.txt`，每条消息写入时间戳和内容。
  - [ ] 处理时间未同步情况（暂存内存，同步后补写）。
  - [ ] 保证写入不阻塞主流程，失败时记录错误日志并降级。
- [ ] **错误日志**
  - [ ] 实现跨模块错误日志环形缓冲，可通过 Web API 查看。

## 阶段 9：测试与优化
- [ ] **单元测试**：测试各外设驱动、音频通路、OpenClaw 协议解析、设备标签解析。
- [ ] **集成测试**：按照 integration-test.md 执行端到端测试（唤醒→对话→TTS→MP3→日志→睡眠唤醒）。
- [ ] **性能调优**：监控内部 RAM 使用量，优化 DMA 缓冲分配；检查音频延迟；减少 WiFi 开销。
- [ ] **稳定性测试**：长时间运行，模拟网络中断、SD 卡插拔、快速连续对话等场景。
- [ ] **功耗测量**：使用 USB 功率计测量不同状态下的电流，优化电池续航。

## 阶段 10：文档与发布
- [ ] **更新 README.md**：添加硬件连接图、编译烧录说明、使用指南。
- [ ] **完善代码注释与 API 文档**。
- [ ] **准备 v1.0.0 发布**。